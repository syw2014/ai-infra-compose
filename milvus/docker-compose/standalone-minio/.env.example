# Milvus MinIO credentials for the bundled MinIO service.
# Change these values before using this in production.
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=F3w%9pZ!Q@7swXk2mA8

# Optional directory for Docker volumes.
# If not set, the current directory is used.
# Example: DOCKER_VOLUME_DIRECTORY=/opt/milvus-data
# DOCKER_VOLUME_DIRECTORY=
